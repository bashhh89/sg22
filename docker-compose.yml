version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3006:3000"
    restart: always
    volumes:
      - ./logs:/app/logs
    environment:
      - NODE_ENV=production 